const fs = require('fs');
const path = require('path');

module.exports = {
  name: 'devreload',
  description: 'Developer-only: reload command modules — text mode removed; use /devmenu',
  developerOnly: true
};
